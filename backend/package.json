{
  "name": "tracker-system-mock",
  "version": "1.0.0",
  "description": "Self-contained Node + Express mock of the betterdays_tracker FastAPI backend (no real backend secrets).",
  "private": true,
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "token": "node scripts/mint-token.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.3.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
